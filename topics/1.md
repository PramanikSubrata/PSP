# Basic knowledge of computer
Unit 1

<!--
  EDIT GUIDE (for Sir / TA):
  - Description: add points one by one -> ### 1. , ### 2. , ### 3. ...
    Inside a point use #### for sub-headings, plus text / lists / tables / code.
  - Notes / Videos / Assignment: one link per line ->  - <title> | "<link>"
    Paste your real link INSIDE the quotes "". Add or remove lines freely.
  - Do NOT use ## Notes / ## Videos / ## Assignment inside the Description.
-->

## Description

### 1. Adding two numbers in C

To add two numbers in C, you declare variables, use `scanf()` to read the user's input, and apply the `+` (addition) operator to calculate the total.

#### C Source Code

```c
#include <stdio.h>

int main() {
    // Declare integer variables for the inputs and the result
    int num1, num2, sum;

    printf("Enter first number: ");
    scanf("%d", &num1);

    printf("Enter second number: ");
    scanf("%d", &num2);

    // Add the two numbers
    sum = num1 + num2;

    printf("The sum of %d and %d is: %d\n", num1, num2, sum);
    return 0;
}
```

#### Expected Output

```
Enter first number: 15
Enter second number: 25
The sum of 15 and 25 is: 40
```

#### Code Explanation
- `#include <stdio.h>` — pulls in the Standard Input/Output library so `printf()` and `scanf()` work.
- `int num1, num2, sum;` — allocates memory for three integer variables.
- `scanf("%d", &num1);` — reads an integer into the variable's memory address (via `&`).
- `sum = num1 + num2;` — the `+` operator adds the two numbers and stores the result.
- `return 0;` — ends the program cleanly and signals success to the operating system.

### 2. Write your next point here

This is where point 2 begins. Write anything — text, a list, a table, or code. Example table:

| Term | Meaning |
|------|---------|
| Variable | A named box that stores a value |
| Operator | A symbol like `+` that performs an action |

<!-- To add point 3, 4, 5 …: copy a "### N." block above, paste below, change the number and content. -->

## Notes
- Note 1 | "https://abc"
- Note 2 | "https://abc"
- Note 3 | "https://abc"

## Videos
- Lecture 1 | "https://abc"
- Lecture 2 | "https://abc"
- Lecture 3 | "https://abc"

## Assignment
- Assignment 1 : deadline | "https://abc"
- Assignment 2 : deadline | "https://abc"
- Assignment 3 : deadline | "https://abc"
- Assignment 4 : deadline | "https://abc"
- Assignment 5 : deadline | "https://abc"
